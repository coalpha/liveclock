{
   "devDependencies": {
      "http-server": "^0.12.1",
      "livescript": "^1.6.0",
      "uglify": "^0.1.5"
   },
   "scripts": {
      "start": "http-server & npm run watch",
      "watch": "lsc -wc index clock",
      "compile": "lsc -c index clock",
      "minify": "uglifyjs index.js -c -m -o index.js && uglifyjs clock.js -c -m -o clock.js",
      "build": "npm run compile && npm run minify"
   }
}
